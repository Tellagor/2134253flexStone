export type CertificateItem = {
  id: string;
  title: string;
  description: string;
  fileHref: string;
};

export type CertificatesSectionContent = {
  title: string;
  subtitle: string;
  downloadAllHref: string | null;
};

